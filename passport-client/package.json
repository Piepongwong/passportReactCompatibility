{
  "name": "passport-client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "start": "REACT_APP_SECRET_BACKEND_URL=http://localhost:3001 react-scripts start",
    "build": "REACT_APP_SECRET_BACKEND_URL=http://localhost:3001 react-scripts build && mv build ../passport-server/react-client",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
